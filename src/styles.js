import { makeStyles } from 'tss-react/mui'


export const useStyles=makeStyles({
     navLogo: {
       display:'flex',
       justifyContent: 'center',
       alignItems: 'center',
    },

    navigDrawer:{
        width:1250,
        color: 'red',
    }

    
});